package com.himanshu.controller;

import org.springframework.web.bind.annotation.RestController;

/**
 * Placeholder — verification endpoints are handled in UserController.
 * Kept to avoid breaking any existing Spring context scans.
 */
@RestController
public class VerificationController {
}
